definitions:
  DriverAbbr:
    type: object
    properties:
      abbr:
        type: string
        item:
          $ref: '#/definitions/Abbr'
  Abbr:
      type: string
      components:
        schemas:
          User:
            Properties:
              name:
                type: string

responses:
  200:
    description: A list of race drivers
    schema:
      $ref: '#/definitions/DriverAbbr'

